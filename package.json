{
  "name": "counting-house",
  "workspaces": [
    "server"
  ],
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "setup": "npm install && npm run setup:server",
    "setup:server": "cd server && npm install",
    "start:server": "npm run dev --workspace=server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}
